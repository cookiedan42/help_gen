// const data = require("./data/grounding.json");
// const data = fetch("./data/grounding.json")
import a from "./data/grounding" 

// const grounding = 

Array.prototype.random = function () {
    return this[Math.floor((Math.random()*this.length))];
  }


function test() {
    // data
    // var repNode = document.getElementById("grounding");
    // var xmlString = grounding.random;
    // var doc = new DOMParser().parseFromString(xmlString, "text/xml");
    // repNode.replaceWith(doc);
    // alert(grounding.random())
}